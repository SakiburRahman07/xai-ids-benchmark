# XAI-IDS Benchmark — pinned dependencies (Python 3.10+, tested on Colab)
# Core
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.5.0
pyyaml==6.0.1
tqdm==4.66.4
joblib==1.4.2

# Deep learning
torch==2.3.1
# torchvision and torchaudio omitted to keep install light; install CUDA build as needed on Colab

# XAI
shap==0.45.1
lime==0.2.0.1
dice-ml==0.1.4
captum==0.7.0

# FT-Transformer: local implementation in src/xai_ids_benchmark/models/ft_transformer.py
# (rtdl PyPI package has conflicting dependencies; not used)

# Analysis / plotting
matplotlib==3.9.0
seaborn==0.13.2
orange3==3.37.0  # optional: for Nemenyi; we also provide a pure-scipy fallback

# Notebook
jupyter==1.0.0
nbformat==5.10.4

# Requests for dataset downloaders
requests==2.32.3
